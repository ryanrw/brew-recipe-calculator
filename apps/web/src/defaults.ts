/**
 * Initial values for the dashboard's controlled inputs.
 * Centralized so the defaults and the picker ranges stay in sync.
 */
export const DEFAULT_COFFEE_GRAMS = 15;
export const DEFAULT_RATIO = 15;
export const DEFAULT_BLOOM_GRAMS = 30;
export const DEFAULT_NUM_POURS = 2;
export const DEFAULT_BLOOM_TIME_SEC = 45;
export const DEFAULT_TOTAL_TIME_SEC = 150;
